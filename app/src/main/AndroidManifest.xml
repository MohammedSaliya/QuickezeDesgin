<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.spiderindia.quickezedesgin">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- LocationsetActivity -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LocationsetActivity" />
        <activity android:name=".AlserActivity" />
        <activity android:name=".DateSelectActivity" />
        <activity android:name=".RecommendedActivity" />
        <activity android:name=".PlumperServiceActivity" />
        <activity android:name=".PlumperActivity" />
        <activity android:name=".AddressPlumperActivity" />
        <activity android:name=".fbGoogleActivity" />
        <activity android:name=".CreditDebitActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".SummaryPlumperActivity" />
        <activity android:name=".PricingAndTermsActivity" />
        <activity android:name=".SignUpActivity"></activity>
    </application>

</manifest>